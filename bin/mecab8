#!/bin/bash

# UTF8 mecab
mecab8 () {
    code=`mecab -D | grep charset:` 
    case "$code" in
    *utf-8*)
		mecab $*
    ;;
    *UTF-8*)
		mecab $*
    ;;
    *euc*)
    	euc_io mecab $*	   
    ;;
    *EUC*)
    	euc_io mecab $*	   
    ;;
    *)
		echo "Uh oh!"
		exit 1
    ;;
    esac
}

mecab8 $*
