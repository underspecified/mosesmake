#!/usr/local/plan9/bin/rc

fn moses_tokenize {
	lang = $1
	switch($lang) {
		case en es
			tokenizer.perl -l $lang >[2=]
		case ja
			tok_ja
	}
}

switch($#*) {
	case 1
		moses_tokenize $1
	case *
		echo usage: moses_tokenize language >[1=2]
		exit 1
}
